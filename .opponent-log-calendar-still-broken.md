# Opponent Log — Calendar still broken (2026-05-24, post-Erik-report)

## CONTEXT (read FIRST every cycle)

**Erik's report — 2026-05-24 06:57 + 07:41**:
> "sorry to report that calendar is not fixed. Same problem still happening in app and on chrome."
> (then provided a landing-page blurb separately — that's been shipped on `a3882d7`)

**Reality check on the prior 6-cycle stale-after-action loop**:
- Loop added `revalidatePath` + `router.refresh()` across 25 sites
- Loop NEVER browser-verified (cycle 6 was explicit: "BLOCKER on browser verification, 4th consecutive cycle")
- Erik tested it on Chrome + the app, it's still broken
- ⇒ The fix is either wrong, incomplete, or addresses a different bug than the one Erik sees
- ⇒ The "same problem" Erik references is not characterized — could be stale-after-action,
  could be sign-in/OAuth, could be event-not-loading, could be PWA cache, could be all
  of the above

**Submodule state (2026-05-24)**:
- `main` HEAD: `a3882d7` (just pushed — includes 6 previously unbumped main fixes:
  SW passthrough removal, instrumentation split, migrations auto-run, bug-report
  enrichment, booking SlotInterval split, window-grid alignment)
- Super-repo pointer also bumped to `a3882d7` (`cc47d9702` in flur_workspace main)
- **A fresh Vercel deploy is now in flight.** First negativa cycle MUST confirm
  the new deploy is live before drawing conclusions about old vs new behaviour.

**The live site**: `https://liminalcalendar.com`
**Worktree**: `C:\flur_workspace\packages\liminal-calendar` (branch: `main`)
**Push to deploy**: yes — Vercel watches submodule `main`

---

## IRON LAW FOR THIS LOOP

**No conclusion about "what is broken" without browser repro.**

The prior loop assumed the bug from the title (stale-after-action) and pattern-matched
fixes onto it without ever opening Chrome on a patched build. That's the failure mode
to avoid. This loop's first three positiva cycles MUST be diagnostic, not fix-first:

1. **Cycle 1 (positiva)**: Open Chrome → liminalcalendar.com. Confirm deploy `a3882d7`
   is live (landing blurb should be Erik's "Welcome to the Liminal Calendar..." text,
   not the prior "A calendar for liminal-web events"). Sign in. Screenshot key flows:
   sign-in, calendar view, RSVP click, event create, event edit, page refresh after
   each action. Annotate which flows show stale state, which throw errors, which
   work. **No code changes this cycle — pure observation.**

2. **Cycle 2 (positiva)**: Pick the worst-observed flow from cycle 1. Investigate
   root cause: network tab, console errors, RSC payload contents, SW state, cookies.
   Identify the actual mechanism. **Still no fix.**

3. **Cycle 3+ (positiva)**: Now fix, with E2E proof each cycle.

---

## CHROME RESILIENCE (mandatory)

```
Step 0 of every cycle:
- mcp__claude-in-chrome__tabs_context_mcp  → check connection
- If empty/error: mcp__claude-in-chrome__tabs_create_mcp → reopen
- If reopen fails: log BLOCKER "Chrome disconnected" and STOP (don't fake evidence)
```

---

## REVIEWER (negativa) CHECKLIST EACH CYCLE

1. `git log --oneline -5` — what did positiva commit
2. `git diff HEAD~1 HEAD` — review the diff
3. Reproduce positiva's browser claim INDEPENDENTLY via Chrome MCP — screenshot
4. Confirm deploy commit matches what positiva claims is "live" (Vercel deploy hash in HTML or console)
5. Prepend entry below — keep last 5 entries only

---

## STIGMERGIC TRACE FOR THE LOOPS

- **Latest commit on branch**: `a3882d7` (2026-05-24 — Erik landing blurb)
- **Issue tracker**: there is no `bd` issue for this — operating direct from Erik's chat report
- **Sister logs to consult**: `.opponent-log-stale-after-action.md` (prior loop's 6 cycles)
  for what's already been tried — but DO NOT assume that bug is the live bug

---

(prepend cycle reviews below)

---

## 2026-05-24T08:08Z — POSITIVA cycle 1: OBSERVATION (no code)

**Goal**: pure browser-based diagnosis. No code edits.

**Setup verified**:
- Submodule main HEAD: `d127889` (log seed) ← `a3882d7` (landing) ← `3de96ec` (instrumentation split) ← `4e98cb3` (auto-migrations) ← `fd82cda` ← `d4c8def` ← `609e5b2` ← `a12dfd5` (SW passthrough removal)
- Live deploy confirmed at `a3882d7`+: curl `https://liminalcalendar.com/` unauth returns 200 with "Welcome to the Liminal Calendar" — Erik's blurb is live.
- Chrome tab: 1025355356, signed in as user `AC` (handle `negativa-probe`).

**Observations** (browser-evidence, not assumed):

1. **React hydration error #418 fires on EVERY load of `/` (signed in)**.
   - Console line: `Error: Minified React error #418; visit https://react.dev/errors/418?args[]=HTML&args[]= ...`
   - Stack: `rD → oq → ik → iu → iX → MessagePort.w` (all in `4bd1b696-f785427dddbba9fb.js`, the Next.js client runtime).
   - React #418 = "Hydration failed because the server rendered HTML didn't match the client. As a result this tree will be regenerated on the client. This won't be patched up."
   - Implication: a Server-Component subtree is being THROWN AWAY and re-rendered client-side after hydration mismatch. If the discarded subtree contained fresh post-action data, the user would see stale data — matches Erik's "same problem". CAUSAL hypothesis, not proven yet.

2. **Authed RSC payloads return 503 INTERMITTENTLY**.
   - Captured live in `read_network_requests` after navigating `/ → /book`:
     - `/?_rsc=1gwfg` → **503** (signed in)
     - `/month?_rsc=1gwfg` → 200
     - `/list?_rsc=1gwfg` → 200
     - `/profile?_rsc=1gwfg` → 200
   - Earlier capture (different prefetch), same browser session:
     - `/book?_rsc=3lb4g` → **503** (signed in)
     - `/settings/booking?_rsc=3lb4g` → **503** (signed in)
   - Curl probes unauthenticated (no session cookie):
     - `/?_rsc=probeN` x3 → 200 / 200 / 200
     - `/book?_rsc=probeN` x3 → 200 / 200 / 200
   - **The 503 is scoped to authenticated users.** Both `/` and `/book` are affected.
   - Implication: `router.refresh()` after an RSVP/edit triggers an RSC fetch of `/`. If that 503s, the UI does NOT update. THAT is the stale-after-action mechanism for any authed user — the prior loop's `revalidatePath` was correct in principle but the route itself fails to render the new payload.

3. **PWA install banner is showing** — service worker is alive. The recent `a12dfd5` removed the SW passthrough fetch handler, but stale SWs in already-installed clients (Erik?) may still hold the old handler until they update. Worth checking on Erik's specific browser later, but not the primary bug.

**Working theory for "calendar is not fixed"** (browser-evidenced):
- Authed RSC handler is throwing/crashing → 503 on prefetch + on `router.refresh()` → mutations succeed server-side but UI never receives the updated payload → user sees stale state.
- The hydration #418 may be a separate, additional bug — or it may be the root cause of the 503 (a Server Component throwing during render returns 500/503).

**Next cycle plan** (for cycle 2):
- Hit `/?_rsc=...` while authed, repeatedly, and capture an actual 503 response BODY (not just status). The body usually has the Next.js error overlay or a stack hint.
- Cross-reference with Vercel function logs if accessible.
- Identify which Server Component is throwing. Likely candidates by code inspection:
  - `expandRecurringEvents(baseEvents, rangeStart, rangeEnd)` — pure compute, but if rrule input is malformed for one row it throws.
  - `dbEventToDisplayEvent(event, rsvps, currentUserId)` — projection; null fields could throw.
  - `<WeeklyGrid events={displayEvents} />` Server Component — if events contain an entry with `null` fields the render throws.
  - The `try/catch` in page.tsx swallows DB errors but does NOT catch render errors from the returned JSX.

**Sticky-constraint audit**: no `--no-verify`, no `git add -A`, no secrets, no fix attempted, observations only.

