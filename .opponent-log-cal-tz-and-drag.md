# Opponent Log — cal-tz-and-drag

**Topic:** Calendar TZ display fix + owner-only drag-to-reschedule
**Spec:** `docs/superpowers/specs/2026-05-03-tz-fix-and-drag-reschedule-design.md`
**Plan:** `docs/superpowers/plans/2026-05-03-tz-fix-and-drag-reschedule.md`
**Worktree:** `.claude/worktrees/cal-tz-and-drag/`
**Branch:** `worktree-cal-tz-and-drag`
**Stop condition:** Negativa declares DONE when all plan checkboxes are ticked, full Jest suite passes, `tsc --noEmit` passes, Chrome MCP E2E confirms (a) owner can drag, (b) recurring shows modal, (c) non-creator admin has no drag handle. Both loops then CronDelete themselves.

---

## Cycle protocol

**Positiva (every 5 min):** read this log → handle BLOCKER/REVERT first → pick next unchecked task in plan → run one TDD cycle (RED → GREEN → typecheck → commit) → tick the box. No reviewing, no refactoring, no skipping.

**Negativa (every 10 min):** read latest commit(s) → run `npx jest` on touched files (full suite on Slice boundaries) → run `npx tsc --noEmit` → check spec/plan conformance + Axiom 1 (no orphan additions) → prepend findings here → KEEP/ADJUST/STOP timer → assess DONE.

Both loops MUST verify Chrome MCP connectivity before claiming UI verification. Disconnect → reconnect or BLOCKER.

---

## Findings (newest first)

(Negativa will prepend reviews above this line.)

---
### Negativa Review — 2026-05-03T (cycle 1, post-A1)
**Status**: ON_TRACK
**Findings**:
- [OK] Commit `3449154` is a pure extraction — zero behavior change. Inline `eventToMinutes` from `DayColumn.tsx` moved to `src/lib/event-time-display.ts` with narrowed input type via `Pick<DisplayEvent, 'starts_at' | 'ends_at'>`. Grep confirms single definition site (no duplicates, no orphans).
- [OK] 4/4 unit tests pass on the touched suite. Cases cover the three branches of the conditional (no `ends_at`, `ends_at <= starts_at`, normal) plus a happy-path 1h event. Pinning is sound — A2 can refactor positioning without losing the math contract.
- [OK] `npx tsc --noEmit` exit 0.
- [OK] Component test suites green (8 suites / 23 tests) — DayColumn's downstream consumers unaffected by the import swap.
- [OK] Plan boxes A1 Steps 1–4 ticked accurately; commit message format matches `positiva: cal-tz-drag — <what> (Task A1)`.
- [WARN] A2's plan stipulates a `suppressMount__test` prop on DayColumn for testability. Acceptable per spec but ugly — if positiva can hit RED with `act()` alone (jsdom flushes useEffect synchronously after render), prefer that and skip the prop. Not blocking, just a discretion call.
- [WARN] A2 also requires adding `data-testid="event-block"` to EventBlock's wrapper. Easy to forget — positiva, double-check the test query target before claiming GREEN.
**E2E Evidence**:
- Tests run: `event-time-display.test.ts` (4/4 pass) + `src/__tests__/components/` (23/23 pass).
- Browser verified: N/A — A1 is a non-UI extraction; the spec only requires Chrome MCP at A3 and B6.
- Chrome status: not engaged this cycle.
**Reflection**:
- Quality trend: baseline (cycle 1) — discipline is correct: RED confirmed before impl, single TDD cycle, no scope creep.
- Pattern observed: positiva narrowed the type to `Pick<...>` rather than copying the full `DisplayEvent` parameter. That's one cleanup beyond the literal plan; reasonable since the helper truly only needs those two fields. Watch for further "while-I'm-here" creep.
- Improvement for next cycle: When A2 lands, run a DOM-level assertion against the *initial* render (not just `findByX` which awaits useEffect) — that's the only way to prove the gate genuinely suppresses SSR-time event blocks rather than just rendering them and immediately re-rendering.
**Ontological Coherence**:
- New terms: none — `eventToMinutes` already existed inline in DayColumn.
- Replaced: inline definition in `DayColumn.tsx` deleted; import added. Clean break per CLAUDE.md "Clean Break Policy". No re-export shim.
- Coherence: coherent. New file `src/lib/event-time-display.ts` is justified by the spec's explicit "Files" table.
**Drift**: TZ flash fix → on plan, A2 is the actual behavior change → aligned
### Timer: KEEP positiva=*/5 negativa=*/10
### Completion: NO — 1/9 plan tasks ticked; A2 is the load-bearing fix.

---
### Init — 2026-05-03
Loops set up. Worktree clean at HEAD `3fdf4b4`. Plan has 9 tasks across 2 slices (A1–A3 TZ fix, B1–B6 drag). Awaiting first positiva cycle.

### Timer: KEEP positiva=*/5 negativa=*/10
### Completion: NO
